<!DOCTYPE html>
<html>
	<head>
		<title>MyWeight</title>
		<%= stylesheet_link_tag :all %>
		<%= javascript_include_tag :defaults %>
		<%= javascript_include_tag 'jquery-1.8.3.min', 'jquery.validate', 'application', 'rails' %>
		<%= csrf_meta_tag %>
	</head>
	<body>
		<div id="user_errors">
			<p class="notice">
				<%= notice %>
			</p>
			<p class="alert">
				<%= alert %>
			</p>
		</div>
		<div id="user_nav">
			<% if user_signed_in? %>
			Signed in as <%= current_user.email %>. Not you?
			<%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
			<% end %>
		</div>
		<%= yield %>

	</body>
</html>
